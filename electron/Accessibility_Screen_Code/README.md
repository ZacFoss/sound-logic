Desktop Accessibility Screen

This folder contains a minimal Electron app that reproduces the Accessibility screen UI from the Flutter project.

Commands:

1. Install dependencies (install electron globally or as a dev dependency):
   npm install --save-dev electron

2. Start the app:
   npm run start

Notes:
- This app uses a simple JSON file (accessibility_state.json) in the same folder to persist settings.
